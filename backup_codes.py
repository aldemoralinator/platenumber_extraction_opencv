

        # firstText = result[0:3]
        # secondText = result[3:]

        # print("++++++++++ start ++++++++++")
        
        # firstText.replace("4", "A")
        # firstText.replace("0", "O")
        # firstText.replace("2", "Z")
        # firstText.replace("5", "S")
        # firstText.replace("6", "G")
        # firstText.replace("8", "B")

        # secondText.replace("A", "4")
        # secondText.replace("O", "0")
        # secondText.replace("Z", "2")
        # secondText.replace("S", "5")
        # secondText.replace("G", "6")
        # secondText.replace("B", "8")

        # if len(firstText) <= 0:
        #     possiblePlate.strChars = ""
        #     continue

        # if len(secondText) < 3 and len(secondText) > 4:
        #     possiblePlate.strChars = ""
        #     continue

        # print("firstText = " + firstText)
        # print("firstText.length = " + str(len(firstText)))
        # print("len(firstText) <= 0 = " + str(len(firstText) <= 0))
        # print("firstText.isalpha() = " + str(firstText.isalpha()))

        # print("secondText = " + "(" + secondText + ")")
        # print("secondText.length = " + str(len(secondText)))
        # print("len(secondText) < 3 and len(secondText) > 4 = " + str(len(secondText) < 3 and len(secondText) > 4))
        # print("secondText.isdigit() = " + str(secondText.isdigit()))

        # print(firstText + secondText)
        # len(firstText) == 3 and len(secondText) >= 3 and len(secondText) <= 4 and firstText.isalpha() and secondText.isdigit()

        # if firstText.isalpha() and secondText.isdigit():
        #     print(firstText + secondText)
        #     possiblePlate.strChars = firstText + secondText
        # else:
        #     possiblePlate.strChars = ""
        # print(result)
        

        # print("++++++++++ end ++++++++++")